import { ByfreelancerconnecterPipe } from './byfreelancerconnecter.pipe';

describe('ByfreelancerconnecterPipe', () => {
  it('create an instance', () => {
    const pipe = new ByfreelancerconnecterPipe();
    expect(pipe).toBeTruthy();
  });
});
